// PDF generator using @react-pdf/renderer
export const certificateGenerator = {
  generatePdfBuffer: async (certificateData) => {
    // Generate and return PDF Buffer
    return Buffer.from([]);
  },
};
